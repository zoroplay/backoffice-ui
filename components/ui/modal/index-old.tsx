// Export the new Modal components
export { Modal, ModalHeader, ModalBody, ModalFooter } from "./Modal";

// Keep the old Modal export for backwards compatibility if needed
export { Modal as LegacyModal } from "./index-old";
